{
	email {$ACME_EMAIL:ssl@ryan.ceo}
}

{$SITE_DOMAIN:openplanet.ryan.ceo} {
	root * /srv
	encode zstd gzip
	file_server
	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		X-Content-Type-Options "nosniff"
		Referrer-Policy "no-referrer"
	}
}
